---
xcode_root: '/Applications'
# Must match one 'version' of an entry of the list variable below
default_xcode: 12.4
# A list variable using the following name and format is required, but not provided (no free lunch)
# xcodes:
#  - { version: <version>, url: <xip_url> }