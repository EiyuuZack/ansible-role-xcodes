---
xcode_root: '/Applications'
# Must match one 'version' of an entry of the list variable below
# For some reason xcodes renames Xcode with a patch digit, so we roll with it
default_xcode: 14.1.0
xcodes:
  - 13.3.0
  - 14.1.0

extra_install_flags:
  - --experimental-unxip
